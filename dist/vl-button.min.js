import {NativeVlElement, define} from '/node_modules/vl-ui-core/dist/vl-core.min.js'; export const VlButtonElement=(t)=>class extends (NativeVlElement(t)) {
  static get _observedAttributes() {
    return [];
  } static get _observedClassAttributes() {
    return ['disabled', 'error', 'block', 'large', 'wide', 'narrow', 'secondary', 'tertiary', 'loading'];
  }connectedCallback() {
    this.classList.add('vl-button'), setTimeout(()=>{
      this._setIconClass();
    });
  } get _classPrefix() {
    return 'vl-button--';
  }_setIconClass() {
    const t=this.querySelector('[is="vl-icon"]'); if (t) {
      let e=''; e+=t.hasAttribute('before')?'-before':'', e+=t.hasAttribute('after')?'-after':'', this.classList.add(this._classPrefix+'icon'+e), t.classList.add('vl-button__icon'), e&&t.classList.add('vl-button__icon-'+e), t.classList.remove('vl-icon--before'), t.classList.remove('vl-icon--after');
    }
  }
}; export class VlButton extends (VlButtonElement(HTMLButtonElement)) {} export class VlLinkButton extends (VlButtonElement(HTMLAnchorElement)) {}define('vl-button', VlButton, {extends: 'button'}), define('vl-link-button', VlLinkButton, {extends: 'a'});
